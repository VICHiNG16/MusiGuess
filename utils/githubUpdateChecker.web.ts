export async function checkForUpdates() {
    // No-op for web
    // This file replaces githubUpdateChecker.ts during web builds
    // avoiding the import of 'expo-application' which may cause build issues.
}
